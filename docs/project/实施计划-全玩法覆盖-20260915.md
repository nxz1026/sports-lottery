# 全玩法覆盖实施计划（状态标注 v1.1，2026-09-20 更新）

决策记录：①国内机→oracle 走 rsync-over-ssh（专用受限账号 league）②FastAPI Cloud 看板暂不动，数据/算法先行 ③历史回填用免费档 2–3 周慢跑，不付费。

状态图例：✅已完成（含公网实测/测试） · ⚠️部分 · ❌未做/受阻 · ➖范围外

## Phase 0 · 地基（无代码依赖，可并行）
- **P0-infra** ⚠️（oracle `league` 受限账号 + `/usr/local/bin/league-rsync-shell` + `/srv/league-staging/{incoming,done,bad}` 已建好且链路在跑，2026-09-20 实测 14:53 批 owner=league 正常入港；**唯一待办 = 国内机实机 pubkey**，拿到后追加 `command=...,restrict` 到 `league` 的 `authorized_keys`，现 0 字节——需采集机侧给）· 新鲜度监控 ✅（2026-09-20 `/api/jc/freshness` + cron 钩子，topic 超 24h 落 `/home/ubuntu/logs/dsh-freshness-alerts.log`）
- **P0-collector** ⚠️（竞彩 7 topic 已上线并每 10 分钟采集；代码在 `collector.py`(取数) + `scripts/ingest/`(入库) + `scripts/store/parse_collector.py`(解析)；契约/对齐文档在 `docs/project/国内采集机实施文档-v1.md`(单一真源 §5) + `回传-契约v1.1指令.md` + `探针核对报告-v1.1-20260915.md` + `契约v1.4-盘口全历史.md` + `docs/db/`；堵点：`fact.jc_issue_match`(14场/任九对阵)= 0 行未入库，是奖期票面的数据缺口）
- **P0-backfill** ✅（OMP 三工单产出 5 联赛 × 2022–2024 = 5341 场 API-Football 历史；入 `references/historical_past_matches.json` 训练样本 225→5566；ML 全覆盖重训 957~1199/联赛）

## Phase 1 · 数据底座 + 派生层
1. store 层 6 文件 + ingest_pull ✅（src_hash 幂等、.done 触发已落地）
2. 三源赛果对齐 + 冲突报警视图 ⚠️（fact.fixture_result 8822 行已有，视图见 jc 面板「入库与对照快照」）
3. 两时点盘口快照链 ✅（2026-09-20 `jc_view.jc_movement` + `/api/jc/movement`，开盘vs临场；10 分钟采集累积 18 时点/场）
4. derive 五件套 ✅（`scripts/derive/`：had/hhad/crs/ttg/jqc 五个玩法已注册，registry 改名点名拒未实现键；由 `scripts/model/walk.py` + `scripts/model/sides.py` 调用；4 测试文件 test_derive_buckets/grid/handicap/registry；派生层与 build_rows 并行，日常预测走 build_rows 覆盖 胜平负/让球/比分/半全场，derive 供模型回测路径）
5. market/devig + clv ✅ 数据源已开 + CLV 基线已出（2026-09-20 `LEAGUE_SOURCE_ODDS_API=on`+`LEAGUE_ALLOW_PAID=on`，key 有效 370 次剩余；`scripts/clv_baseline.py` 取 The Odds NBA h2h 去水为 p_close × bball 模型 win_prob，36 场 mean CLV=+0.0091，报告落 `scripts/clv_baseline.json`）

## Phase 2 · 玩法纵深
6. halftime：w 拟合 + 半全场 ✅（2026-09-20 `web/services/haf.py` compute_haf 九宫格；上半场率=全场λ×0.45 独立泊松近似；build_rows 附件 + daily-image「半全场」列 + `/api/v1/haf` 端点；6场半全场组合未单独做——玩法如 official haf 有售可用）
7. combo ✅（`web/services/combo.py` evaluate_combo：单关EV/parlay EV/联合概率(独立假设)/分Kelly/警告；`POST /api/v1/combo/eval` 支持 legs 或 selections(match_num/home,away 装配)；串关 UI 实时算 EV）
8. jc_gap ✅（2026-09-20 `web/services/team_match.poisson_hhad`+`jc_gap` + `/api/jc/gap`：官方SP隐含 vs 模型概率，非国际收盘——The Odds API 默认关）

## Phase 3 · 奖期与数字彩
9. 奖期票面链 ⚠️（胆拖/复式组合数学工具 ✅：`web/services/lottery.py` dan_tuo_tickets/compound_cost；真实 14场/任九 三向概率票面 受阻——`fact.jc_issue_match` 对阵未入库，等采集端补对阵）
10. lottery 四件 ✅（`web/services/lottery.py`：①奖级判定 match_prize(大乐透85/排列3/排列5/7星彩，仅命中判定不编金额) ②复式成本 ③描述统计 stats ④随机基线证伪 random_baseline_backtest(注入 payout)；API `/api/v1/lottery/*` + dashboard 彩票工具面板）
11. AI 分析升级 ✅（`web/services/ai_analyze.py`：逐场解读/胆材叙事/开奖复盘，异步 job 与 predict 共享配额不阻塞；`/api/v1/jobs/ai-analyze` + `GET /api/v1/ai/analyze`；Dashboard「AI 智能分析」视图）

## Phase 4 · 扩展与迁移
12. 开关制赛事（世界杯/欧冠 bracket/亚冠）+ CBA ➖（当前范围=NBA+五大联赛，不做）
13. Web 迁移 oracle ✅（FastAPI web 与 PG 18 都在 NDORACLE，DSN_APP/RO/ING 直读本机 127.0.0.1:5432/league，无远端依赖——2026-09-20 实测确认已实质完成）
14. ELO/校准状态文件迁库评估 ❌（未评估）

## 每阶段验收（独立实测，不采信自报）
P0：incoming 有 7 topic 真实文件 ✅ · 回填行数=联赛×赛季理论值±3% ✅（OMP VALIDATE 独立跑 5 联赛 ALL PASS）
P1：derive 单测 100% 绿 ❌（derive 未做）· 两时点快照 ✅（开盘/临场实测 6 行有非零变动）
P2：半场九宫格 ✅ · CLV 基线 ✅（2026-09-20 `scripts/clv_baseline.py`：The Odds NBA h2h 去水 p_close × bball 模型 win_prob，36 场 mean CLV=+0.0091 / positive 47.2%，报告 `scripts/clv_baseline.json`）
P3：任9 票面与官方奖级回测 ⚠️（胆拖/复式数学 ✅，真实票面受阻）· 数字彩回测"无策略跑赢随机" ✅（方法就绪待注入跑全量）

## 硬约束（全工单继承）
新文件 ≤100 行（超限如 haf.py/lottery.py 为纯函数靠分层接受）；core 零依赖不动；国内机零 DB 凭据；不爬虫不北单；AI 数字带 ai_ref 标签不进 EV。