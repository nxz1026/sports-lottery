# league-v2 当前 main 验收报告（2026-09-20）

- 日期：2026-09-20
- 验收对象：`/home/ubuntu/league-v2/repo`，分支 `main`，起点提交 `09bb110`
- 线上地址：https://140.83.62.161/dashboard/jc/
- 验收方式：真浏览器（Playwright + Chromium headless shell）逐视图核对 + 线上 API 实测 + 独立全量测试
- 与 09-18 报告（`3d8c812..ea50244 v2`，581 测试）对比：当前 main = 09-18 修复 + 一系列后续 feat/fix，含 haf/lottery/combo/jc_gap/movement/freshness/daily-image 增强

---

## 一、结论摘要

**全部通过。** 7 视图 × 3 视口（desktop / laptop / narrow 390px）= 21 组合，**0 console 错误、0 水平溢出**；原有 13 个 `/api/v1` 端点全部 200；新增 9 个端点（haf / lottery / combo / ai-analyze + jobs / jc_gap / jc_movement / jc_freshness）实测 200 且业务值正确；边界校验 400 正确；全量 **721 passed**（基线 546 + 历次新增 175）。

| 维度 | 结果 |
|---|---|
| git 状态 | HEAD `09bb110` == `origin/main`，无 ahead/behind |
| 全量 pytest | 721 passed, 2 warnings, rc=0 |
| 7 视图 × 3 视口 | console 0 错 / 水平溢出 0 |
| 原有 13 API | 全部 200 |
| 新增 9 API | 全部 200（含边界 400） |
| 每日一图渲染 | canvas 900×1240，19 场五大联赛 7 列（含新增「半全场」列），含 15 行 haf_model / 15 行 hhad_model，命中率 15/28 |

---

## 二、git 状态确认（HEAD 干净 + 全部已 push）

```
## main...origin/main
09bb110 docs: 更新 plan/README 标注完成项 + 采集端代码与对齐文档索引
```

- HEAD 09bb110 == origin/main，无 ahead/behind，未推送内容已全部上远端
- 报告 09-18 起点 `3d8c812` / 交付 `ea50244` 是 main 的祖先（main 历史包含 09-18 的 P0 修复）

---

## 三、7 视图 × 3 视口 实测（playwright headless）

3 视口：`desktop 1440×900`、`laptop 1280×720`、`narrow 390×844`（窄屏手机）。

| 视口 | console 错 | 水平溢出 |
|---|---|---|
| desktop | 0 | 0 |
| laptop | 0 | 0 |
| narrow (390px) | 0 | 0 |

具体逐视图结果：21 组合 = 7 视图 × 3 视口，**全部 0 console 错误、0 水平溢出**。

---

## 四、关键 API 实测（独立 curl + 断言，不采信自报）

### 4.1 原有 13 个 `/api/v1` 端点（覆盖报告 §3 列出的 API 基线）

```
200 /api/v1/predictions/today          keys=date,leagues
200 /api/v1/championship              keys=leagues
200 /api/v1/accuracy                   keys=leagues,pending
200 /api/v1/accuracy/breakdown        keys=leagues
200 /api/v1/calibration               keys=leagues
200 /api/v1/history                   keys=leagues
200 /api/v1/backtest                  keys=rows,sql_note,degraded
200 /api/v1/prediction-metadata       keys=leagues
200 /api/v1/ai/daily                  keys=available,date,summary,items
200 /api/v1/ai/ranking                keys=available,date,metric,definition,matched_count,hot
200 /api/v1/ai/status                 keys=available,matches,by_league,source
200 /api/v1/sources/status            keys=configured,enabled,default_source,leagues,jobs
200 /api/v1/me                        keys=username,authenticated
```

全部 200。

### 4.2 新增端点（haf / lottery / combo / jc_gap / jc_movement / jc_freshness / ai_analyze）

```
200 GET  /api/v1/haf?home=佛罗伦萨&away=那不勒斯       pick=平负, probs 9 键齐全
200 POST /api/v1/haf {home,away,line:0}                  pick=平负, probs_sum=0.9999
200 POST /api/v1/haf {match_num:7012,line:-1}            home=曼彻斯特城 away=桑德兰 pick=胜胜
200 GET  /api/v1/lottery/prize?game=85&ticket&draw       prize=二等奖, status=win
200 GET  /api/v1/lottery/cost?game=85&front&back         注数=18, 2元成本=36, 拆法=C(6,5)×C(3,2)
200 GET  /api/v1/lottery/dantuo?dan&tuo&choose           注数=10, 公式=C(5,3)
200 GET  /api/v1/lottery/stats?game=35&n=20              periods=20, stats 5 字段齐全
200 POST /api/v1/combo/eval {2 腿}                       n_legs=2, parlay_ev=-0.2, kelly=0
200 GET  /api/jc/gap?top=5                              items=0（最新业务日 09-21 亚运非五大联赛，无错位列）OK
200 GET  /api/jc/movement                               items=0（同上；09-20 五大联赛全部开赛前无变动）OK
200 GET  /api/jc/freshness?threshold_hours=24           ok=True, stale=0, alerts=0（8 个 topic 全新鲜）
200 GET  /api/v1/ai/analyze?date=2026-09-20            available=True, per_match=14, lottery=4
```

### 4.3 边界校验（应 400）

```
400 POST /api/v1/combo/eval {legs:[...], joint_prob:2.0}   msg="joint_prob 必须在 [0, 1]"  ✓
400 POST /api/v1/haf {home:"佛罗伦萨"}                    msg="需提供 home/away 或 match_num"  ✓
```

---

## 五、每日一图渲染（含新增「半全场」列）

- canvas：900×1240（无报错，client JS 0 错）
- 数据：19 行五大联赛 / 15 行带 `haf_model` / 15 行带 `hhad_model` / 命中率 15/28（30 天）
- 表格 7 列：联赛 / 场次 / 对阵 / 让球 / 推荐 / 比分 / **半全场**（新增列）
- 半全场列填充（实测样例）：
  - 佛罗伦萨 vs 那不勒斯 → 平胜
  - 勒沃库森 vs 莱比锡红牛 → 平负
  - 拉齐奥 vs 皇家贝蒂斯 → 负胜
  - 帕尔马 vs 热那亚 → 负胜
- 底部 QR + 链接（指向 dashboard 主页）+ 合规标语
- **LLM 要闻降级**：当前 daily-news 返回 fallback 占位文（"竞彩足球今日有多场五大联赛在售，详情见下表"），原因是外部 LLM client 直调返回 `{}`（LLM 服务不可达）。这是 graceful degradation，非回归：daily_news 设计走 LLM 失败→占位，不影响主预测链路。LLM 恢复后会回到真实 3 行角度短讯。

---

## 六、与 09-18 报告（v2）对比

09-18 报告交付 `ea50244`（3 个 P0 修：取数窗口 / 命中率连接键 / 蒙特卡洛），在当前 main 是祖先；09-18 报告列出的"未做/待办"在当前 main 的复核：

| 09-18 报告遗留项 | 当前 main 状态 | 处理 |
|---|---|---|
| `fact.jc_issue_match` / `jc_issue_prize` / `jbq_match` / `jbq_offer` = 0 行 | ✅**足球侧 2 张已通**（`jc_issue_match` 0→238、`jc_issue_prize` 0→23，commit `b28cdd3`，2026-09-20 cron 14:40 实测入库）；`jbq_match`/`jbq_offer` 仍 0（篮彩，范围外：Phase4-12 CBA/开关制赛事不做，且 `parse_jclq_offer` §5.5 未冻结） | 奖期真实票面数据缺口已解（14场/任九对阵进库）；篮彩留待 10 月开售窗口复探 |
| 「仅显示关注联赛/球队」chip 死代码 | ✅**已修**（commit `705da52`）：`matchPasses` 读 `state.personalized` → `followPasses`；卡片新增 ○关注/◎关注中 按钮；`followSummary` 显示关注数与启用态；Playwright 验证 52→5 过滤正确 | 逐个对账 |
| `fact.fixture` 真实比赛更新 ELO | 已落库（阿森纳 1557.3→1706.4 在 09-18 提交中）| 持续累积 |
| `parse_jclq_offer` raise | 仍 raise | 契约 §5.5 未冻结 |
| 命中率 1-2 天数据积累 | 现在历史样本 28 场（30 天窗口）；ML 全覆盖 | 持续累积中 |
| `Number(null)=0` 静默吃掉 NBA confidence_score null | 已修（指 09-18 修复的一部分，已是 main 祖先）| 持续生效 |

---

## 七、测试与交付

```
$ PYTHONPATH=scripts:. /home/ubuntu/.venvs/league/bin/python -m pytest -q
734 passed, 2 warnings               rc=0
FAILED 行数 = 0
基线 721 + 新增 13（test_jc_issue_match_prize.py）= 734  ✔
```

新增单测（最近轮次）：
- `tests/test_freshness.py`（7） — evaluate_topics / format_alert_lines / sort
- `tests/test_combo.py`（9） — 单关 EV / parlay / 联合独立假设 / Kelly / 边界
- `tests/test_ai_analyze.py`（4） — 三类生成 / 异常隔离 / CLI
- `tests/test_haf.py`（5） — 九宫格求和 / 让球 / 非法输入 / 零线
- `tests/test_lottery.py`（9） — 组合数学 / 奖级 / 复式 / 胆拖 / 描述统计
- `tests/test_haf_router.py`（4） — endpoint 命中 / 反查 / 边界

### 防诈审查
- 本次验收（09-20 基线）仅查（playwright + curl + pytest），**不修改生产代码**；提交 `09bb110`。
- **追加修复（同日晚，已 commit + push）**：
  - `705da52` — 修复 chip 死代码（`matchPasses` 读 `state.personalized` + follow 过滤 + 关注按钮）
  - `b28cdd3` — `jc_issue` 展开 matchList → `jc_issue_match`、prizeLevelList → `jc_issue_prize`
  - 两 commit 均已 push 到 origin/main（HEAD = `b28cdd3` == origin/main），工作树干净。

### 改动文件
- 验收基线：无。
- 追加修复：
  - `scripts/store/parse_jcissue.py` / `parse_collector.py` — parser 多指令展开
  - `scripts/ingest/jc_issue_write.py` — COLUMNS 加 2 表 + prize 纯 INSERT
  - `scripts/ingest/jc_topic.py` — `_load_saved` 拉平 list
  - `static/dashboard.html` — 关注 chip 生效
  - `tests/test_jc_issue_match_prize.py`（13 新）+ `test_jbq_topic_wire.py`/`test_jc_topic_issue.py`（适配）
  - `scripts/visual/test_dashboard_follow_chip.py`（Playwright）
  - `docs/project/` — 计划陈旧标记更正 + `给国内机-交接-league-pubkey-20260920.md`

---

## 八、未做 / 仍阻塞
1. **`jbq_match` / `jbq_offer` 仍为 0** —— 篮彩（jclq_offer §5.5 未冻结；10 月开售窗口复探后升 v1.2 再解析）。属范围外（Phase4-12 开关制赛事 + CBA ➖）。足球侧 14场/任九 已通过 `jc_issue_match`（238）/`jc_issue_prize`（23）入库解开。
2. **daily-news LLM 当前不可达**（`ai.llm_client.generate` 返回 `{}`）—— 外部依赖问题，恢复后自动回到真实 3 行角度短讯。需关注上游 LLM 服务稳定性。
3. **P0-infra 收尾**：oracle `league` 受限账号/目录/链路已建好在跑，**唯一待办 = 国内机实机 pubkey**（`authorized_keys` 现 0 字节），已写交接说明 `docs/project/给国内机-交接-league-pubkey-20260920.md`。
4. **计划文档陈旧标记已更正**：derive 五件套 ❌→✅（`scripts/derive/` 已注册 had/hhad/crs/ttg/jqc 并被 walk.py/sides.py 调用）；P0-infra ❌→⚠️（差国内机 pubkey）。
