# league-v2 本轮收尾处置记录（2026-09-20）

> 供用户次日核验。本轮三件事：①打开 The Odds API（出 CLV 基线的前置）②用已入库 jc_issue_match
> 实现 14场/任九 三向真实票面 ③无头浏览器到生产 `/dashboard/jc/` 元素级逐一测试并修问题。

## 一、The Odds API 已打开 ✅

- `.env`（gitignored，不提交）：`LEAGUE_SOURCE_ODDS_API=on` + `LEAGUE_QUOTA_CAP_ODDS_API=20` + **`LEAGUE_ALLOW_PAID=on`**（新增）
- key `e9945bff…` 有效（probe 到 75 sports、NBA 在列、剩余 370 次）
- `spend_allowed('odds_api')` 通过；`describe()` 显示 odds_api `enabled=True cap=20 paid=True spend=True`
- **CLV 基线前置已解**：计划 P1-5 / P2 的 CLV 基线待注入官方/盘口数据跑全量（`scripts/core/backtest.py` 已有 direction/score/ou 命中口径）

## 二、14场/任九 三向真实票面 ✅（commit `70b8c34`）

- `web/services/ticket.py`（纯函数）：`sfc_picks` 逐场推荐 / `parallel_prob` 联合概率 / `combine_prob` 任九 top9 命中概率 / `choose_combinations`
- `web/routers/ticket.py`：`GET/POST /api/v1/ticket/sfc`，读 `fact.jc_issue_match` 对阵 + 预测 `reasoning_factors`/`ml_proba` 三向概率
- `scripts/store/jc_view.py`：新增 `issue_matches(game_num, issue_no?)` 逐场查询
- 16 单测（test_ticket 10 + test_ticket_router 6）
- 真库 EPL 期 26131 验证：14 场 10 场有模型概率（伯恩茅斯vs利物浦→客胜0.474 正确；曼城vs桑德兰→主胜0.612），任九 2002 组合命中概率 0.46%；当前期 26132（热身赛）无匹配标 no_probs 不编数

## 三、生产元素级测试 ✅（重启服务后 13/13 过）

- **服务已重启**：`league-dashboard.service`（systemd）加载新代码 + 新 `.env`（ticket 端点 + odds_api 生效）
- 无头浏览器（nginx basic admin/ndjack + 页面 a/a）：
  - 今日卡片 52 场 ✓ · ticket/sfc 端点 live（issue=26132 n=14）✓
  - **关注 chip（任务 B）生产可用**：关注→summaries 更新→个性化过滤 5/52→关闭恢复 52 ✓
  - 全部 7 主视图 + lottery + JC 6 tab 渲染正常 ✓
- 生产 URL：`https://140.83.62.161/dashboard/jc/` 公网 IP 本机不可达（hairpin），以 `https://127.0.0.1` 等价验证（同一 nginx+uvicorn）

## 四、记录的分歧/已知项（未修，供用户拍板）

1. **`/api/v1/me` 未登录探测 401 console 噪音**：`_probeAuthAndBoot` 在页面登录前 fetch `/me` 判定会话，401 会记一条浏览器 console error，但 JS 已优雅处理（显示"未登录"），**不影响功能**。属浏览器对任何 401 响应的固有 console 记录，非回归。若要根治需改成读 session cookie 而不发 401 请求，属行为变更，未顺手改。
2. **CLV 基线报告本身还未出**：odds_api 已开、key 有效，但需再注入官方/盘口数据跑全量基线报告（本机有 bball/odds_api.py 取数 + scripts/market/clv.py 计算，未实操全量）。
3. **当前传统足彩期 26132 是国际热身赛**（中国/马尔代夫、澳大利亚/巴西），不在五大联赛预测集 → 票面 n_with_probs=0（honest no_probs，非 bug）；五大联赛期（如 26131 EPL）能正常出票面。

## 五、git 状态

- `1b3c22f`（上次推送后：plan 更新）已 push；HEAD == origin/main
- 本轮代码 commit：`705da52`(B chip) `b28cdd3`(A 采集4表) `70b8c34`(ticket) `1b3c22f`(plan)
- 本地 750 pytest 全绿；工作树干净