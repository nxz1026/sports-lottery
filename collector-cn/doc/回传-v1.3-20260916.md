## 契约 v1.3 回传（国内采集机 → 海外）2026-09-16

### 1. `.done` 清单 `topic/` 前缀丢失 —— 已修复
- 根因：`_manifest()` 写相对路径时用了裸文件名（`p.name`），tar 解包侧用的是 `topic/` 前缀（`p.parent.name/p.name`）⇒ 清单里的路径和落盘目录对不上，远端 loader 全部回退到哈希匹配（每批 440 条告警）。
- 修复：`rel = f"{p.parent.name}/{p.name}"`（collector.py `_manifest`），commit 待推。
- 验证：新批 `2026-09-16T09-58-31Z.done` 8 行全部 `topic/xxx.jsonl` 形式，第 3 列哈希非空。

### 2. `jc_odds_history` 新 topic —— 已上线，定时 8-topic 批次
- 端点：`uniform/football/getOddsHistoryV1.qry?channel=c&matchId=<id>`（见 `doc/赔率走势接口说明.md`，无 Referer/Cookie 要求，探针 200/ok）。
- 语义：一行 = 一场**在售**足球（matchStatus=="Selling"）× 整条赔率走势。payload = `value` dict 原样全搬（`hadList`/`hhadList`/`crsList`/`ttgList`/`hafuList`/`singleList` + `matchId`/`homeTeamId`/`awayTeamId`/`leagueId` 等元键），**零裁剪**。
- matchId 来源：先取 `getMatchCalculatorV1`（jczq_offer 同端点）在售场次，逐场 GET history（QPS=1.0 节流，27 场 ≈ 60s）。
- 外壳 `topic` 字段 = `jc_odds_history`（**已纠正一次性样本的 `jczq_offer` 误标**），目录名同。
- 当前在售 27 场；10m 批次耗时约 70s，节奏可承受。
- 远端 `fact.jc_odds_history` + 写手已可接收；新批 `.done` 已含该 topic 行。

### 3. `jclq_result` 键结构 + 开售/完赛各 1 完整样本
- 落盘：`jclq_sample_review/jclq_result_key_structure_and_samples.json`（4264B，已 scp 到远端）。
- payload 22 键：`allAwayTeam/allHomeTeam/awayTeam/awayTeamId/finalScore/hdc/hilo/homeTeam/homeTeamId/leagueBackColor/leagueId/leagueName/leagueNameAbbr/matchDate/matchId/matchNum/matchNumStr/matchTime/mnl/poolStatus/status/wnm`。
- `status`：`1`=开售中（finalScore="-"），`2`=完赛（finalScore="主-客"连字符）。
- **NBA/CBA 确认**：篮球接口**不过滤联赛，混出**——当前 7 天窗内 `jclq_result` 实际 leagueId={7: 女篮世界杯, 8: 亚运会男篮}；`jclq_offer` 在售 5 场**全部 leagueId=26（美国女子篮球联盟/WNBA）**。即：**NBA/CBA 场次只在赛程窗口内出现（NBA 10 月开赛），当前时段官方接口在售的就是 WNBA/亚运**。接口本身不按联赛过滤，开赛后自然带出 NBA/CBA。

### 4. "14 个无主文件"（15:03 起）—— 答复
- 全量核查：远端 7 个 topic 目录共 **312 个文件**不在任何 `.done` 清单内（不只是 14 个；14 应是某统计窗口口径）。
- **根因就是第 1 条**：清单缺 `topic/` 前缀 ⇒ 所有 15:03 后批次文件（`jc_issue`/`jc_issue_result`/`jczq_result`/`lottery_draw`/`jclq_offer.empty` 等）都成了"无主"。前缀修复后，远端重扫一次即可全部归位，**不需要补写历史 `.done`**。
- 唯一真·无主：`odds_history_sample/`（3 文件）——一次性采样包，设计上不走批次清单，建议远端按只读对照处理。

### 5. `.done` 第 3 列哈希 —— 生效中
- 非 `.empty` 行 = 逐行 sha256 级联（官方公式），实测 `2026-09-16T09-58-31Z.done` 8 行哈希全部非空。

### 部署说明
- B1 批次 7 topic → **8 topic**（+`jc_odds_history`）。计划任务 5 个（`collector_0930/1530/2130/2330/offer_10m`）Exec 均指向 `collector_silent.vbs`，VBS 内 topic 串已加 `jc_odds_history` ⇒ **无需重建任务**，下一轮自动生效。
- 契约版本 `collector.py` `CONTRACT = "v1.3"`，commit 随本次代码一起推。
