# 回传：国内采集机 v1.1 首批验收（队长，2026-09-15 15:25Z）

收到 3 批：`2026-09-15T14-40-02Z`（227 行）/ `15-03-45Z`（85 行）/ `15-16-32Z`（138 行），共 **450 行**。
**总评：PASS，可入库**（远端解析器 450/450 零异常，无需你们改字段）。下列 4 项要改 + 1 项要你们自查，另有 1 处我们测得的数字与你们报告不一致。

## A. 我复算通过的（不用动）
- `src_hash`：**450/450 与官方式复算一致**（`sha256(json.dumps(payload,ensure_ascii=False,sort_keys=True,separators=(",",":")))`）⇒ "零清洗"成立；
- `snap_ts`：**同一 topic 文件内同值、跨 topic 不同值**（实测 14:35 批有 5 个：:37/:39/:40/:45/:49 ⇒ 别拿 snap_ts 当批身份，见 G 节）、UTC 带 `Z`（14:35:37Z / 15:03:39Z / 15:16:3xZ）；`http_status` 全 200；`endpoint`/`collector_host` 齐；
- 切分粒度：`jczq_offer` = **17 场 × 5 块 = 85 行**，`block` 值域恰为 `had/hhad/crs/ttg/hafu`；`CRS.options` 66 键、`TTG` 20 键、`HAFU` 23 键（多一个 `id`）、`HAD/HHAD` 各 10 键；
- 身份键：offer/result = `matchId`；`lottery_draw` 120 行的 `(lotteryGameNum,lotteryDrawNum)` **零重号**（4 个玩法各 30 期：`85`超级大乐透 / `35`排列3 / `350133`排列5 / `04`7星彩）；`jc_issue` 跨玩法同期重号（90 与 900129 同为 26127）**带 lotteryGameNum，正确**。

## B. 必须改（4 项）
1. **每批 7 个 topic 必须齐全**：`.jsonl` 或 `.empty` 二选一，缺一个都不行。实测 15:03 批只有 `jczq_offer`+`jclq_offer.empty`；15:16 批**缺 `jczq_offer`、`jc_issue`、`jclq_offer` 三个 topic**（既无数据也无 `.empty`）。远端无法区分"抓了但没变化"和"这一轮没跑"——盘口时序因此出现断档。
2. **`.done` 必须是最后一步**：`15-03-45Z.done` 写于 15:03:45，而该批 `jczq_offer` 文件同分钟才落；远端由 `.done` 触发入库，早写=漏主题。请改成"全部 topic 写完 → 各文件 `.tmp` → rename → 最后 rename `.done`"。
3. **10 分钟档我确认要上，但只上 offer 两个 topic**（`jczq_offer`/`jclq_offer` 每 10 分钟一次；result/issue/lottery 维持 §6 的 4 时点）。理由见 D 节：盘口价值全在时序密度。
4. **外壳第 9 键 `fetched_at`**：我在 §5.0 冻结的是 8 键，你们多给了 `fetched_at`。**已升契约 v1.2**，请把 `collector.status.json` 的 `contract_version` 回写为 `v1.2`（远端按版本号判断解析分支）。**裁决：保留**（我要用它测响应延迟），已把契约改成 9 键，但必须满足：不进 `src_hash`、不当身份键、格式与 `snap_ts` 同为 UTC 带 `Z`、**不许用它顶替 snap_ts**。

## C. 请你们自查一处 + 数字更正一处
- **让球线位置**：你们写"hhad goalLine=`-1` 让球线"。实测 **payload 顶层没有 `goalLine`（取值为 null）**，让球线在 `options.goalLine` / `options.goalLineValue`（真样例：`+1` / `+1.00`）。若你们在顶层做过提升/复制 ⇒ 违反"零清洗"，请回退；若只是描述口径，回一句确认即可。
- **键数口径**：你们报"`jczq_offer` payload 22 键"，我测到 **26 键**（22 + `block`/`poolCode`/`options`/`oddsHistory`）。请统一口径，别再出现"22/26/40"三种数。
- **`kind=error` 路径这三批一次都没触发**（全 200 且 `errorCode="0"`）。请在下次遇到 `P0001`/`success!=true` 时保留样例行回传，这是目前唯一没被真包验证过的分支。

## D. 为什么 10 分钟档这么要紧（给你们看证据）
14:35 → 15:03 两批对比：**85 行里只有 6 行 `src_hash` 变了，而这 6 行水一个都没动**——变的全是 `*f` 升降盘箭头标志（`1/-1 → 0`）。
⇒ ① 真实价格变动是**稀疏事件**，靠低频抓取会全部错过；② 远端做盘口时序时**只认价格键**（`h/d/a/s0..s7/…`），`*f` 与 `updateTime` 一律当噪声，所以你们不必为"标志变了"专门产新批，但**定长 10 分钟不能停**。
另一条白捡的：`oddsHistory` 每行带 **5 条历史价**（含 `updateDate/updateTime`、`goalLine`、`poolCode/poolId`）⇒ 即使我方快照断档，也能回填最近 5 个变盘点，第一版 CLV 可以先吃这个。

## E. 官网核对（只能靠你们）
我这台机器 `webapi.sporttery.cn` **直连返回 HTTP 567（反爬挡海外 IP）**，所以我做的是内部一致性核对，不是与官网对照。请你们在官网页面上逐条核这 10 行，回一张"官网显示值 / 一致或不一致"的表：
1. `2041485` 周二006 阿布艾因 vs 利雅胜利 — HAD `h3.75 d3.90 a1.65`；2. 同场 HHAD `+1 / +1.00`，`h1.94 d3.75? a2.88`；3. 同场 TTG `s2 4.40 / s3 3.80 / s4 4.50`；4. 同场 HAFU（`aaf` 由 1→0）；5. `2041487` 周二008 阿拉维斯 vs 巴伦西亚 TTG `s2 3.10 / s3 3.60 / s5 7.00`；6. `2041490` 周二011 利物浦 vs 热刺 HAFU（6 个 `*f` 归零）；
7. `2041482` 周二003 卡塔尔亚 vs 韩国亚 **半场 0:3 / 全场 1:4**，SP `h18.00 d7.20 a1.07`，`winFlag=A`、`poolStatus=Payout`、`matchResultStatus=2`；
8. `2041484`/`2041483` 周二005/004 —— `sectionsNo999` 为**空串**（未开奖/取消？）请说明官网此刻显示什么；
9. `jc_issue` `(90, 26127)` 胜负游戏：14 场，首站 `中国亚 vs 朝鲜亚`（`gmMatchId=2041494`，`startTime=2026-09-16`）；
10. `lottery_draw` 排列3（`gameNum=35`）任一近三期内：**两批之间 `prizeLevelList` 结构变了**（`[{awardType,group…}]` → `[{prizeLevel:'直选',…}]`）且 `lotterySaleEndTimeUnix` 由 `0` 变成 `{}`。这是官方在开奖前后换结构，远端按"最新覆盖 + 类型容错"处理；请确认**不是你们抓错期**（同一 `(gameNum,drawNum)` 两次内容不同）。

## F. 我们这边的动作（告知）
夹具将改为**直接从真包抽样**（原来的夹具来自 `getMatchListV1` 探针，比生产多 18 个键，已不足以代表真实形状）；`fact.jc_*` 八表 DDL 已就位，下一步 `P0-COLLECT2` 做 450 行 → 八表的写入与幂等回放。探针包分子目录不急，下次复探时一起改。

---

## G. **v1.3 必须补的一条：`批 ↔ 文件` 现在无法从名字关联**（队长 2026-09-16 03:39Z 逐目录核对发现）

现实布局（`incoming/cn-collector/`，也就是我方 `/tmp/incoming` 的副本）：

```
2026-09-15T14-40-02Z.done      2026-09-15T15-03-45Z.done      2026-09-15T15-16-32Z.done
jczq_offer/2026-09-15T14-35Z__001.jsonl   jczq_offer/2026-09-15T15-03Z__001.jsonl
jczq_result/2026-09-15T14-35Z__001.jsonl  ...（共 7 个 topic 目录）
```

两个问题：
1. **`.done` 的名字是"上传完成时刻"（14:40:02），数据文件的名字是"采集分钟"（14-35Z）**，两者相差约 4 分钟且**没有任何共同字段** ⇒
   远端**无法从名字判断某个 `.done` 覆盖哪些文件**。文档 §6 原话是"完成后 touch **同名** `.done`"，现实实现不是同名。
2. 更隐蔽的一点：**同一批内 `snap_ts` 跨 topic 不同**（实测 14:35:37 / :39 / :40 / :45 / :49 五个值），
   所以"批"也**不能**靠 `snap_ts` 反推——只能靠"每个 topic 文件内部 `snap_ts` 相同"这个性质。

我方**曾**采用的兜底口径：`.done` 按名字升序 + **数据文件 `mtime ≤ 该 .done 的 mtime`** 即算属这批。
**这条已经在现实中死过一次**（2026-09-16 04:38Z 我方 `stat` 实测：包内所有 `.done` 与全部数据文件的 mtime **完全相同** = `2026-09-15 15:38:26 +0000`，
也就是 mtime 被「解包/搬运」抹平了 ⇒ 拿 mtime 分批会把**三批全部塞进每一批**，而且**不会报任何错**。
⇒ 现在改用**文件名时间戳窗口**：`.done` 名解析出 `t_i`，第 i 批 = `(t_{i-1}, t_i]` 区间内的文件；实测与你们三批的真实分布**逐条吻合**。
**但这仍是我们的猜测口径**，且有一个明确弱点：它只能认「**每个 topic 一个文件**」——同一窗口内若出现 `__002` 分片，我方 loader 现在会**静默只取最后一个文件**。
所以：
**请 v1.3 二选一并回写文档**（任选即可，我方按结果改 ingest）：
· **(A) 强推荐**：`.done` **文件内写清单**（它现在是 0 字节空文件，白放着）：
  每行 `<相对路径>\t<行数>\t<该文件内容的 src_hash 聚合>`（至少要有相对路径与行数），远端以清单为准；
  · 并请明确 **同一批同一 topic 是否可能出现 `__002` 分片**；若可能，清单就是**唯一**正确解法（窗口口径只能取最新，会丢数据）。
· **(B)** 数据文件名改用**与 `.done` 同一个批次戳**：`<batch>/jczq_offer__001.jsonl` 或直接 `jczq_offer/2026-09-15T14-40-02Z__001.jsonl`
  （把 `fetched_at` 那一批的统一上传戳打进文件名），恢复"同名 `.done`"的原文语义。
另：`probe_pack.tar.gz` 请别再往 `incoming/` 里放（我方 loader 会忽略它，但它在契约目录里就是噪声）。

---

## H. **篮彩要开卖了：请补一批 `jclq_offer` / `jclq_result` 真包**（2026-09-16 用户定范围后新增，**优先级顶到 B 节前面**）
老板 06:35 定了范围：篮球只做 **NBA / CBA**，所以 §5.5「v1.1 不冻结篮彩」现在**必须冻结**。
你们三批真包里 `jclq_offer` / `jclq_result` **全是 `.empty`**（10:00 / 15:03 / 15:16 三个时点确实无盘，不是采集失败），
⇒ 远端**一个字段都没见过**，没法凭猜测建表（足球那边猜一次 `jc_id` 就炸一晚上）。

**请做两件事（都是一次性的）**：
1. **抓一批有盘的篮彩真包**：NBA/CBA 开售时段（北京时间 12:00 之后到凌晨，竞彩篮彩一般到 22:00 封盘前都有盘）
   跑一次 `jclq_offer` + `jclq_result`，**按 §5 同样的外壳**（9 键、`src_hash` 官方式、UTF-8、一行一对象、`.done` 最后写）推进来；
   麻烦顺带告知你们用的 **endpoint 全名与查询参数**（足球那边钉死是 `getMatchCalculatorV1.qry?channel=c`，篮球我要同样确定的东西）。
2. **回答三个问题**（写进你们的 `PROVENANCE` 或回一句话都行）：
   · `jclq_offer` 一行是"一场 × 一个玩法"还是"一场多玩法挤在 payload 里"？玩法有哪几种（胜负 / 让分 / 大小分 / 胜负分区…）？
   · 让分线、总分线放在哪个键（足球的坑：顶层 `goalLine` 是 null，真值在 `options.goalLine` / `options.goalLineValue`）？
   · `jclq_result` 里半场比分、加时、**让分/大小分的"盘口结算值"** 是哪几个键？（篮彩结算跟足球不一样，我要原始形状）

拿到真包后我这边按足球的流程走：**建 `fact.jbq_*` → 解析层 → 装载 → 与官网核对（10 行核对表我出，你们执行）**。
数字彩那四个（大乐透 / 排列3 / 排列5 / 七星彩）**保持现状即可**，老板确认只作开奖对照、不进模型，`lottery_draw` 已经在收。
